{% extends '_templates/base.njk' %}

{% block page_title %}Movies{% endblock %}

{% block page_description %}A list of movies that I love.{% endblock %}

{% block content %}
<article class="container mt-5">
  <h1>Four Movies That Mean the Most to Me</h1>
  <p>These are four movies that mean a lot to me — not just because they’re great films, but because they’re tied to personal memories, people I love, and moments in my life. Each one has stuck with me in its own way, and this page is a space to share why.</p>

  <div class="container">
    <div class="mt-5">
      <div class="row row-cols-1 row-cols-md-2 g-4 mt-5">
        {% for movie in movies.celeste %}
        <div class="col">
          <div class="card h-100">
            <div class="row g-0 h-100">
              <div class="col-md-4">
                <img src="img/{{ movie.image }}" class="img-fluid rounded-start h-100 w-100" alt="{{ movie.title }}" style="object-fit: cover;">
              </div>
              <div class="col-md-8">
                <div class="card-body d-flex flex-column h-100">
                  <h5 class="card-title">
                    <a href="reviews/{{ movie.slug }}.html" class="text-decoration-none">{{ movie.title }}</a>
                  </h5>
                  <p class="card-text"><strong>About: </strong>{{ movie.about }}</p>
                  <p class="card-text mt-auto">
                    <strong>Rating:</strong> {{ movie.rating }}<br>
                    <small class="text-body-primary">Directed by {{ movie.director }}</small>
                  </p>
                </div>
              </div>
            </div>
          </div>
        </div>
        {% endfor %}
      </div>
    </div>
  </div>
</article>
{% endblock %}