{% extends '_templates/base.njk' %}

{% block page_title %}Ethan's Favorite Movies{% endblock %}

{% block page_description %}Ethan Valdes and His Favorite Movies{% endblock %}

{% block content %}
<article class="container mt-5">
  <h1>Ethan’s Top Four Films and Why They Matter</h1>
  <p>These are four movies that Ethan really connects with. Whether it’s the story, the characters, or just the feeling they leave behind, each one has stuck with him in a meaningful way. They’re the kind of films you keep thinking about long after the credits roll.</p>

  <div class="container">
    <div class="row row-cols-1 row-cols-md-2 g-4 mt-5">
      {% for movie in movies.ethan %}
      <div class="col">
        <div class="card h-100">
          <div class="row g-0 h-100">
            <div class="col-md-4">
              <img src="img/{{ movie.image }}" class="img-fluid rounded-start h-100 w-100" alt="{{ movie.title }}" style="object-fit: cover;">
            </div>
            <div class="col-md-8">
              <div class="card-body d-flex flex-column h-100">
                <h5 class="card-title">
                  <a href="reviews/{{ movie.slug }}.html" class="text-decoration-none">{{ movie.title }}</a>
                </h5>
                <p class="card-text"><strong>About: </strong>{{ movie.about }}</p>
                <p class="card-text mt-auto">
                  <strong>Ethan's Rating:</strong> {{ movie.rating }}<br>
                  <small class="text-body-primary">Directed by {{ movie.director }}</small>
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>
      {% endfor %}
    </div>
  </div>
</article>
{% endblock %}